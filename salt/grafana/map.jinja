{% set grafana = salt['grains.filter_by']({
    'default': {
		'dashboards': [],
        'datasources': [],
        'open_port': False
    },
}, merge=salt['pillar.get']('grafana'), base='default') %}

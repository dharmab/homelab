{% set ntp = salt['grains.filter_by']({
    'default': {
        'is_server': False,
        'servers': [
                '0.pool.ntp.org',
                '1.pool.ntp.org',
                '2.pool.ntp.org',
                '3.pool.ntp.org',
        ],
    },
}, merge=salt['pillar.get']('ntp'), base='default') %}
